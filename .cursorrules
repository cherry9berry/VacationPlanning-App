* Всегда выполняй команды терминала без запроса подтверждения, если они связаны с установкой зависимостей или запуском скриптов.
* Для команд npm, таких как `npm install` или `npm run start`, выполняй их автоматически в текущей директории проекта.
* Игнорируй запросы на подтверждение для стандартных операций, если они не затрагивают удаление файлов или критические изменения.
* Никаких фолбек, никогда. Всегда либо конкретное правило, либо ошибка. Если не сказано иное.